# Coq/Rocq build artifacts
*.vo
*.vok
*.vos
*.glob
*.aux
.*.aux
.lia.cache
.nia.cache

# Generated by `rocq makefile -f _CoqProject -o Makefile` — contains local paths
Makefile
Makefile.conf
.Makefile.d

# (paper.pdf is committed so GitHub can preview it inline)

# Rocq native compilation
.coq-native/

# Local README (not for public repo)
README.md

# Audit compiled artifacts (source is kept; compiled output isn't)
results/AssumptionsAudit.vo
results/AssumptionsAudit.glob
